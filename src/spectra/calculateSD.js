export function calculateSD(sèectra) {
  let result = [];
  for (let i = 0; i < spectra.numberOfPoints; i++) {
    let arrayForSD = [];
    for (let entry of this.spectra) {
      arrayForSD = entry.y[i];
    }
    result.push();
  }
}
